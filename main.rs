use std::env;
use std::fs::File; //ファイルを扱うライブラリ
use std::io::prelude::*; //ファイル入出力ライブラリ
std::io::BufReader; 

struct FileHeader{
    u8:data[file_header_size];
    
}

fn main() {
    println!("Hello, world!");
}
